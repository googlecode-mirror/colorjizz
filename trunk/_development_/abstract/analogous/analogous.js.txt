analogous : function(include){
  if (include == null) include = false;
  var current = this.toCIELCh();
  var distance = 30;
  var rtn = [
    this.hue(distance)[this.toSelf](),
    this.hue(-distance)[this.toSelf]()
  ];
  if (include) rtn.unshift(this);
  return rtn;
}